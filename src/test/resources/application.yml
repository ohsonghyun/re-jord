spring:
  profiles:
    active: local
  jpa:
    open-in-view: false

rejord:
  jwt:
    sort-key: ${SORT_KEY:sort}
    access-token:
      period: 1800000 # ms
    refresh-token:
      period: 3600000 # ms
