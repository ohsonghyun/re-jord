spring:
  profiles:
    active: test
  jpa:
    open-in-view: false

rejord:
  jwt:
    sort-key: ${SORT_KEY:sort}
    access-token:
      period: 1800000 # ms
    refresh-token:
      period: 2592000000 # ms
